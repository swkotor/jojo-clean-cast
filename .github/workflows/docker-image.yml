name: Docker Image CI



on:
  release:
    types: [created]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/go-podcast-sponsor-block:${{ github.event.release.tag_name }}
            ${{ secrets.DOCKER_USERNAME }}/go-podcast-sponsor-block:latest
            ${{ secrets.DOCKER_USERNAME }}/clean-cast:${{ github.event.release.tag_name }}
            ${{ secrets.DOCKER_USERNAME }}/clean-cast:latest
